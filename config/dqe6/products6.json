{
    "expect_or_drop": {
        "valid_product_id": "product_id IS NOT NULL",
        "no_rescued_data": "_rescued_data IS NULL"
    },
    "expect_or_quarantine": {
        "quarantine_rule": "_rescued_data IS NOT NULL OR product_id IS NULL OR price < 0 OR price > 50000 OR stock_quantity < 0"
    }
}
