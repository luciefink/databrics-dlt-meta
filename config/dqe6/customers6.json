{
    "expect_or_drop": {
        "valid_customer_id": "customer_id IS NOT NULL",
        "no_rescued_data": "_rescued_data IS NULL"
    },
    "expect_or_quarantine": {
        "quarantine_rule": "_rescued_data IS NOT NULL OR customer_id IS NULL OR age < 0 OR age > 120 OR (email IS NOT NULL AND NOT email RLIKE '^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\\\\.[A-Z|a-z]{2,}$')"
    }
}
